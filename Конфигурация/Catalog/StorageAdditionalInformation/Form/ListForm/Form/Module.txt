
&AtClient
Procedure OnOpen(Cancel)
	
	Message(NStr("en = 'Editing images in the list is not available'; ru = 'Редактирование изображений в списке не доступно'"));

	Cancel = True;
	
EndProcedure
