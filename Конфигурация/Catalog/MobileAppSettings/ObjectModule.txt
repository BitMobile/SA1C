
Procedure OnWrite(Cancel)
	
	If Not Cancel Then
		
		If ThisObject.Ref = Catalogs.MobileAppSettings.CreateReturnsEnabled Then
			
			Constants.Returns.Set(ThisObject.LogicValue);
			
		EndIf;
		
	EndIf;
	
EndProcedure
