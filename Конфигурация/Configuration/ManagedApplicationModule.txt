
// Структура, содержащая в себе время начала и окончания обновления программы.
Перем ПараметрыРаботыКлиентаПриОбновлении Экспорт;

Процедура ПриНачалеРаботыСистемы()
	
	МодульРегламентныеЗаданияСлужебныйКлиент = Вычислить("РегламентныеЗаданияСлужебныйКлиент");
	МодульРегламентныеЗаданияСлужебныйКлиент.ПриНачалеРаботыСистемы();
	
КонецПроцедуры

Procedure BeforeStart(Cancel)
	ОшибкаЗапускаСПустымСпискомПользователей = ПользователиСлужебныйВызовСервера.ПроверитьВозможностьЗапускаСПустымСпискомПользователейИБ(Истина);
	Если Не ПустаяСтрока(ОшибкаЗапускаСПустымСпискомПользователей) Тогда
		Отказ = Истина;
		ПользователиСлужебныйКлиент.СоздатьАдминистратораИлиЗавершитьРаботу(
			ОшибкаЗапускаСПустымСпискомПользователей);
		Возврат;	
	КонецЕсли;
		
EndProcedure




